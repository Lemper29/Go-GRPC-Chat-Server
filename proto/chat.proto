syntax = "proto3";

package todo;

option go_package = "./proto";

service ChatService {
    rpc JoinChat(stream Chat) returns (stream Chat);
}

message Chat {
    string user = 1;
    string message = 2;
}